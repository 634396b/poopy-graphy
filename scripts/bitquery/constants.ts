const path = require('path')
export const outFolder = 'core/bitquery'
export const outSchema = path.join(outFolder, 'generated/bitquery.graphql')
export const outQueries = path.join(outFolder, 'generated/queries.graphql')
